/* ===========================
   Úkoly — Seznam
=========================== */

var TasksList = {
  render(container) {
    container.innerHTML = `<div style="color:var(--text-4);margin-top:40px;text-align:center;font-size:13px;">Seznam — připravuje se...</div>`;
  }
};
