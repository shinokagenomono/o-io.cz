/* ===========================
   Finance — Přehled
=========================== */

var Finance = {
  render(container) {
    container.innerHTML = `<div style="color:var(--text-4);margin-top:40px;text-align:center;font-size:13px;">Finance — připravuje se...</div>`;
  }
};

var FinancePlan     = { render(c) { c.innerHTML = `<div style="color:var(--text-4);margin-top:40px;text-align:center;font-size:13px;">Roční plán — připravuje se...</div>`; } };
var FinanceSavings  = { render(c) { c.innerHTML = `<div style="color:var(--text-4);margin-top:40px;text-align:center;font-size:13px;">Spoření — připravuje se...</div>`; } };
var FinanceShopping = { render(c) { c.innerHTML = `<div style="color:var(--text-4);margin-top:40px;text-align:center;font-size:13px;">Nákupy — připravuje se...</div>`; } };
var FinanceInvest   = { render(c) { c.innerHTML = `<div style="color:var(--text-4);margin-top:40px;text-align:center;font-size:13px;">Investice — připravuje se...</div>`; } };
