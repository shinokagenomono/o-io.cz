/* ===========================
   Homelab — Přehled
=========================== */

var Homelab = {
  render(container) {
    container.innerHTML = `<div style="color:var(--text-4);margin-top:40px;text-align:center;font-size:13px;">Homelab — připravuje se...</div>`;
  }
};
